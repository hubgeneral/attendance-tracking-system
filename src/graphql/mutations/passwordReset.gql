mutation ResetPassword(
  $userName: String!
  $newPassword: String!
  $token: String!
) {
  resetPassword(username: $userName, token: $token, password: $newPassword) {
    isPasswordReset
    message
  }
}
